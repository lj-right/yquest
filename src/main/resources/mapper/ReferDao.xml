<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suifeng.yquest.dao.ReferDao">

    <resultMap type="com.suifeng.yquest.entity.Refer" id="ReferMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="company" column="company" jdbcType="VARCHAR"/>
        <result property="refercode" column="referCode" jdbcType="VARCHAR"/>
        <result property="industry" column="industry" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="sort" column="sort" jdbcType="INTEGER"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="url" column="url" jdbcType="VARCHAR"/>
        <result property="createdBy" column="created_by" jdbcType="VARCHAR"/>
        <result property="createdTime" column="created_time" jdbcType="TIMESTAMP"/>
        <result property="updatedBy" column="updated_by" jdbcType="VARCHAR"/>
        <result property="updatedTime" column="updated_time" jdbcType="TIMESTAMP"/>
        <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ReferMap">
        select
        id, company, referCode, industry, name, email, sort, description, url, created_by, created_time, updated_by,
        updated_time, is_deleted
        from refer
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ReferMap">
        select
        id, company, referCode, industry, name, email, sort, description, url, created_by, created_time, updated_by,
        updated_time, is_deleted
        from refer
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="company != null and company != ''">
                and company = #{company}
            </if>
            <if test="refercode != null and refercode != ''">
                and referCode = #{refercode}
            </if>
            <if test="industry != null and industry != ''">
                and industry = #{industry}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="sort != null">
                and sort = #{sort}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
            <if test="url != null and url != ''">
                and url = #{url}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and created_by = #{createdBy}
            </if>
            <if test="createdTime != null">
                and created_time = #{createdTime}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and updated_by = #{updatedBy}
            </if>
            <if test="updatedTime != null">
                and updated_time = #{updatedTime}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from refer
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="company != null and company != ''">
                and company = #{company}
            </if>
            <if test="refercode != null and refercode != ''">
                and referCode = #{refercode}
            </if>
            <if test="industry != null and industry != ''">
                and industry = #{industry}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="email != null and email != ''">
                and email = #{email}
            </if>
            <if test="sort != null">
                and sort = #{sort}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
            <if test="url != null and url != ''">
                and url = #{url}
            </if>
            <if test="createdBy != null and createdBy != ''">
                and created_by = #{createdBy}
            </if>
            <if test="createdTime != null">
                and created_time = #{createdTime}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and updated_by = #{updatedBy}
            </if>
            <if test="updatedTime != null">
                and updated_time = #{updatedTime}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
        </where>
    </select>

    <select id="countByCondition" resultType="java.lang.Integer">
        select count(*) from refer
        where is_deleted = 0
    </select>

    <select id="queryAllRefer" resultMap="ReferMap">
        select
        id, company, referCode, industry, name, email, sort, description,url, created_by, created_time, updated_by,
        updated_time
        from refer
        where is_deleted = 0
        limit #{start},#{pageSize}
    </select>
    <select id="countByDelCondition" resultType="java.lang.Integer">
        select count(*) from refer
        where is_deleted = 1
    </select>

    <select id="queryAllDelRefer" resultMap="ReferMap">
        select
        id, company, referCode, industry, name, email, sort, description,url, created_by, created_time, updated_by,
        updated_time
        from refer
        where is_deleted = 1
        limit #{start},#{pageSize}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into refer(company, referCode, industry, name, email, sort, description, url, created_by, created_time,
        updated_by, updated_time, is_deleted)
        values (#{company}, #{refercode}, #{industry}, #{name}, #{email}, #{sort}, #{description}, #{url}, #{createdBy},
        #{createdTime}, #{updatedBy}, #{updatedTime}, #{isDeleted})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into refer(company, referCode, industry, name, email, sort, description, url, created_by, created_time,
        updated_by, updated_time, is_deleted)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.company}, #{entity.refercode}, #{entity.industry}, #{entity.name}, #{entity.email},
            #{entity.sort}, #{entity.description}, #{entity.url}, #{entity.createdBy}, #{entity.createdTime},
            #{entity.updatedBy}, #{entity.updatedTime}, #{entity.isDeleted})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into refer(company, referCode, industry, name, email, sort, description, url, created_by, created_time,
        updated_by, updated_time, is_deleted)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.company}, #{entity.refercode}, #{entity.industry}, #{entity.name}, #{entity.email},
            #{entity.sort}, #{entity.description}, #{entity.url}, #{entity.createdBy}, #{entity.createdTime},
            #{entity.updatedBy}, #{entity.updatedTime}, #{entity.isDeleted})
        </foreach>
        on duplicate key update
        company = values(company),
        referCode = values(referCode),
        industry = values(industry),
        name = values(name),
        email = values(email),
        sort = values(sort),
        description = values(description),
        url = values(url),
        created_by = values(created_by),
        created_time = values(created_time),
        updated_by = values(updated_by),
        updated_time = values(updated_time),
        is_deleted = values(is_deleted)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update refer
        <set>
            <if test="company != null and company != ''">
                company = #{company},
            </if>
            <if test="refercode != null and refercode != ''">
                referCode = #{refercode},
            </if>
            <if test="industry != null and industry != ''">
                industry = #{industry},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="url != null and url != ''">
                url = #{url},
            </if>
            <if test="createdBy != null and createdBy != ''">
                created_by = #{createdBy},
            </if>
            <if test="createdTime != null">
                created_time = #{createdTime},
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                updated_by = #{updatedBy},
            </if>
            <if test="updatedTime != null">
                updated_time = #{updatedTime},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from refer where id = #{id}
    </delete>

</mapper>

